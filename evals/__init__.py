"""Evaluation experiments for lode.

Each subpackage is a self-contained experiment runnable via
``python -m evals.<name>.run``. Experiments live outside ``src/`` and may
depend on heavyweight datasets (see the ``evals`` dependency group).
"""
